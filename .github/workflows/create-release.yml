name: Create release
on:
  workflow_dispatch:
    inputs:
      tag_name:
        type: string
        required: true
        description: 'Existing tag for the release (created using fossil)'

jobs:
  validate-tag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Verify tag exists
        run: |
          if ! git rev-parse -- "refs/tags/${{inputs.tag_name}}" >/dev/null 2>&1; then
            echo "Tag \"${{inputs.tag_name}}\" does not exist!"
            exit 1
          fi

  build-linux-kits:
    uses: ./.github/workflows/build-linux.yml
    needs: validate-tag

  build-windows-kits:
    uses: ./.github/workflows/build-windows.yml
    needs: validate-tag

  build-macos-kits:
    uses: ./.github/workflows/build-macos.yml
    needs: validate-tag

  create-release:
    runs-on: windows-latest
    defaults:
      run:
        shell: msys2 {0}

    permissions:
      contents: write

    needs:
      - build-linux-kits
      - build-windows-kits
      - build-macos-kits

    steps:
      - uses: msys2/setup-msys2@v2
        with:
          msystem: CLANG64
          update:  true

      - uses: actions/checkout@v4
      - name: Download all kits
        uses: actions/download-artifact@v4

      - name: Create release
        run: |
          cp ./kitcreator-windows-kits/tclkitsh-8.6.15-x86_64-w64-mingw32-minimal.exe ~/tclkit.exe
          chmod +x ~/tclkit.exe
          ~/tclkit.exe .github/scripts/create-release.tcl \
              ${{ github.api_url }} \
              ${{ github.repository_owner }} \
              ${{ github.event.repository.name }} \
              ${{ inputs.tag_name }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
