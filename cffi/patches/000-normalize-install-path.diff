diff -ruN A/Makefile.in B/Makefile.in
--- A/Makefile.in	2024-12-01 02:21:03.000000000 -0400
+++ B/Makefile.in	2025-08-26 13:36:44.279864200 -0300
@@ -414,15 +414,7 @@
 	@list='$(lib_BINARIES)'; for p in $$list; do \
 	  if test -f $$p; then \
 	    ext=`echo $$p|sed -e "s/.*\.//"`; \
-	    if test "x$(MACHINE)" = "x"; then \
-	        todir="$(DESTDIR)$(pkglibdir)"; \
-	    elif test "x$(MACHINE)" = "xAMD64"; then \
-		todir="$(DESTDIR)$(pkglibdir)/win32-x86_64"; \
-	    elif test "x$(MACHINE)" = "xX86"; then \
-		todir="$(DESTDIR)$(pkglibdir)/win32-ix86"; \
-	    else \
-		todir="$(DESTDIR)$(pkglibdir)/$(MACHINE)"; \
-	    fi; \
+	    todir="$(DESTDIR)$(pkglibdir)"; \
 	    echo " $(INSTALL_LIBRARY) $$p $$todir/$$p"; \
 	    $(INSTALL_LIBRARY) $$p "$$todir/$$p"; \
 	    if test "x$$ext" = "xdll"; then \
